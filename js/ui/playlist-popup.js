import { PlaylistPopup } from "./path/to/playlist/PlaylistPopup.js";

window.PlaylistPopup = new PlaylistPopup(
  playbackController,
  events,
  albumLibrary,
);

export { PlaylistPopup };
export default PlaylistPopup;
